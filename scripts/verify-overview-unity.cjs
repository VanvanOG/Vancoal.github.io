const{chromium}=require('C:/Users/86183/.cache/codex-runtimes/codex-primary-runtime/dependencies/node/node_modules/playwright');const assert=require('node:assert/strict');const fs=require('node:fs');
(async()=>{const b=await chromium.launch();try{const p=await b.newPage({viewport:{width:1440,height:1000},reducedMotion:'reduce'});await p.goto('http://127.0.0.1:5174/');await p.locator('.startup-loading').waitFor({state:'detached',timeout:45000});await p.getByRole('button',{name:'查看项目'}).click();await p.locator('.project-intro-project-layer.is-interactive').waitFor();for(let i=0;i<2;i++)await p.getByRole('button',{name:'Next project',exact:true}).click();const button=p.getByRole('button',{name:'Open AI 委托玩法',exact:true});await button.hover();assert.equal(await button.locator('.lucide-play').count(),1,'Commission must retain play icon');assert.equal(await button.evaluate(e=>getComputedStyle(e).cursor),'pointer');
 fs.mkdirSync('qa/overview-unity',{recursive:true});for(const width of [1440,1920,768,390,360]){await p.setViewportSize({width,height:1000});const positions=[];for(const slug of ['mars-era','habitat-ai-dialogue','ai-commission']){await p.goto('http://127.0.0.1:5174/#/projects/'+slug);await p.locator('h1').waitFor();await p.waitForTimeout(150);const title=await p.locator('h1').boundingBox();positions.push(title.x);assert.equal(await p.locator('#top .project-menu').count(),1);assert.equal(await p.locator('#top .timeline-node').count(),3);assert.equal(await p.evaluate(()=>document.documentElement.scrollWidth>innerWidth),false);await p.locator('#top').screenshot({path:`qa/overview-unity/${slug}-${width}.png`});if(slug==='ai-commission'){const widths=await p.locator('.commission-image.detail').evaluateAll(es=>es.map(e=>e.clientWidth));assert.ok(widths.every(w=>w<=420));}}assert.ok(Math.max(...positions)-Math.min(...positions)<2,'Aligned title columns: '+positions);console.log(width+' overview alignment PASS');} }finally{await b.close()}})().catch(e=>{console.error(e);process.exitCode=1});
